version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - bundle install
        - npm ci
    build:
      commands:
        - npm run deploy
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths: []
